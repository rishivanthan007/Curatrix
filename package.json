{
  "name": "@curatrix/workspace",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run build && node --test tests/curatrix.test.js tests/mcp-contract.test.js",
    "lint": "tsc -b --pretty false --noEmit",
    "curatrix": "node packages/cli/dist/index.js",
    "mcp:start": "tsx packages/mcp-server/src/index.ts",
    "link": "npm run build && npm link --workspace curatrix",
    "pack:dry": "npm run pack:dry --workspace curatrix"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
